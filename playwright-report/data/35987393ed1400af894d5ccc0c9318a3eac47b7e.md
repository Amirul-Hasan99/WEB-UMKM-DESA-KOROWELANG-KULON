# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: public-flow.spec.ts >> Portal UMKM Desa Korowelang Kulon - End-to-End User Flow >> 4. User can open Feedback page and fill out submission form
- Location: e2e\public-flow.spec.ts:32:7

# Error details

```
Test timeout of 30000ms exceeded.
```

```
Error: page.goto: net::ERR_ABORTED; maybe frame was detached?
Call log:
  - navigating to "http://localhost:3000/feedback", waiting until "load"

```

# Test source

```ts
  1  | import { test, expect } from '@playwright/test';
  2  | 
  3  | test.describe('Portal UMKM Desa Korowelang Kulon - End-to-End User Flow', () => {
  4  |   test('1. Homepage loads successfully with branding title and hero CTA', async ({ page }) => {
  5  |     await page.goto('/');
  6  |     
  7  |     // Verify Navbar Brand Title
  8  |     await expect(page.locator('header')).toContainText('UMKM Korowelang Kulon');
  9  |     
  10 |     // Verify Hero Section
  11 |     await expect(page.locator('h1')).toBeVisible();
  12 |   });
  13 | 
  14 |   test('2. User can navigate to UMKM Catalog and search for products', async ({ page }) => {
  15 |     await page.goto('/umkm');
  16 | 
  17 |     // Check header catalog title
  18 |     await expect(page.locator('h1')).toContainText('Katalog');
  19 | 
  20 |     // Fill search input
  21 |     const searchInput = page.locator('input[placeholder*="Cari"]');
  22 |     if (await searchInput.isVisible()) {
  23 |       await searchInput.fill('Bandeng');
  24 |     }
  25 |   });
  26 | 
  27 |   test('3. User can navigate to About page and view village information', async ({ page }) => {
  28 |     await page.goto('/tentang');
  29 |     await expect(page.locator('h1')).toBeVisible();
  30 |   });
  31 | 
  32 |   test('4. User can open Feedback page and fill out submission form', async ({ page }) => {
> 33 |     await page.goto('/feedback');
     |                ^ Error: page.goto: net::ERR_ABORTED; maybe frame was detached?
  34 | 
  35 |     // Fill Form Inputs
  36 |     await page.fill('input[name="name"], input[placeholder*="nama" i]', 'Budi Santoso');
  37 |     await page.fill('input[name="email"], input[placeholder*="email" i]', 'budi.santoso@example.com');
  38 |     await page.fill('textarea', 'Saran dari warga untuk pengembangan fasilitas pemasaran UMKM desa.');
  39 |   });
  40 | 
  41 |   test('5. Admin user can open Login page', async ({ page }) => {
  42 |     await page.goto('/login');
  43 |     
  44 |     await expect(page.locator('input[type="email"]')).toBeVisible();
  45 |     await expect(page.locator('input[type="password"]')).toBeVisible();
  46 |   });
  47 | });
  48 | 
```